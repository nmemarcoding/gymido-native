import PlaceholderScreen from '../placeholders/PlaceholderScreen';

// Placeholder until the onboarding (profile creation) spec arrives.
export default function OnboardingScreen() {
  return <PlaceholderScreen title="Onboarding" />;
}
